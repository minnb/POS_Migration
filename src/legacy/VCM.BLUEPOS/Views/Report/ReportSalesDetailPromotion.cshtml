@using VCM.BLUEPOS.Model.Order;
@using VCM.BLUEPOS.Model.POS;
@using VCM.BLUEPOS.Model.Store;
@using VCM.BLUEPOS.Model.OptionModel;
@using VCM.BLUEPOS.Model.Common;

@{
    ViewBag.Title = "Báo cáo chi tiết khuyến mãi";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var listSetDB = ViewBag.SetDB as List<SQLServerModel>;
    var listOrderType = ViewBag.ListOrderType as List<OptionDataModel>;
    var listSalesType = ViewBag.ListSalesType as List<OptionDataModel>;
    var listSourceBill = ViewBag.ListSourceBill as List<OptionDataModel>;

}

<div class="subheader py-2 py-lg-6 subheader-transparent" id="kt_subheader">
    <div class="container d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
        <div class="d-flex align-items-center flex-wrap mr-1">
            <div class="d-flex align-items-baseline flex-wrap mr-5">
                <ul class="breadcrumb breadcrumb-transparent breadcrumb-dot font-weight-bold p-0 my-2 font-size-lg">
                    <li class="breadcrumb-item text-muted">
                        <a style="color:#0C713D;">Vận hành</a>
                    </li>
                    <li class="breadcrumb-item text-muted">
                        <a style="color:#0C713D;">Báo cáo khuyến mãi</a>
                    </li>
                    <li class="breadcrumb-item text-muted">
                        <a style="color:#0C713D;">Báo cáo chi tiết khuyến mãi</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>


<div class="d-flex flex-column-fluid">
    <div class="container">
        @using (Html.BeginForm("", "Report", FormMethod.Get, new { @id = "frm-export-excel" }))
        {
            <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xl-12">
                    <div class="card card-custom bgi-no-repeat gutter-b card-stretch">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Từ ngày<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <div class="input-group">
                                            <div class="input-group-prepend">
                                                <label class="input-group-btn" for="txt-from-date">
                                                    <span class="btn btn-default">
                                                        <span class="glyphicon glyphicon-calendar">
                                                            <i class="far fa-calendar-alt"></i>
                                                        </span>
                                                    </span>
                                                </label>
                                            </div>
                                            <input type="text" class="form-control float-right" id="txt-from-date" name="FromDate" placeholder="Từ ngày">
                                        </div>
                                    </div>
                                </div>

                                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Đến ngày<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <div class="input-group">
                                            <div class="input-group-prepend">
                                                <label class="input-group-btn" for="txt-to-date">
                                                    <span class="btn btn-default">
                                                        <span class="glyphicon glyphicon-calendar">
                                                            <i class="far fa-calendar-alt"></i>
                                                        </span>
                                                    </span>
                                                </label>
                                            </div>
                                            <input type="text" class="form-control float-right" id="txt-to-date" name="ToDate" placeholder="Đến ngày">
                                        </div>
                                    </div>
                                </div>

                                <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Vùng Set<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="input-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-set-server" name="IPSetServer">
                                            @if (listSetDB != null && listSetDB.Count > 0)
                                            {
                                                if (listSetDB.Count == 1)
                                                {
                                                    <option value="@listSetDB.FirstOrDefault().IPServer" data-serverread="@listSetDB.FirstOrDefault().IPServerRead">@listSetDB.FirstOrDefault().Description</option>
                                                }
                                                else
                                                {
                                                    foreach (var item in listSetDB)
                                                    {
                                                        <option value="@item.IPServer" data-serverread="@item.IPServerRead">@item.Description</option>
                                                    }
                                                }
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="col-lg-4 col-md-6 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">ST/CH<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-store-no" name="StoreNo"></select>
                                    </div>
                                </div>
                            </div>

                            <div class="row">

                                @*<div class="col-lg-3 col-md-6 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Thuế suất (%)<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-vat-code" name="VatCode">
                                            <option value="" selected>Tất cả</option>
                                            <option value="4">10%</option>              <!--- Code SQL: 4 = 10% -->
                                            <option value="5">8%</option>               <!--- Code SQL: 5 = 8% -->
                                            <option value="3">5%</option>               <!--- Code SQL: 3 = 5%-->
                                            <option value="2">0%</option>               <!--- Code SQL: 2 = 0% -->
                                            <option value="6">Non Tax</option>          <!--- Code SQL: 6 = Khong xuat hoa don -->
                                        </select>
                                    </div>
                                </div>*@

                                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Loại đơn hàng<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-order-type" name="OrderType">
                                            @if (listOrderType != null && listOrderType.Count > 0)
                                            {
                                                if (listOrderType.Count == 1)
                                                {
                                                    <option value="@listOrderType.FirstOrDefault().Code">@listOrderType.FirstOrDefault().Description</option>
                                                }
                                                else
                                                {
                                                    <option value="" selected>Tất cả</option>
                                                    foreach (var item in listOrderType)
                                                    {
                                                        <option value="@item.Code">@item.Description</option>
                                                    }
                                                }
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important">Hình thức bán hàng<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-sales-type" name="SalesType">
                                            @if (listSalesType != null && listSalesType.Count > 0)
                                            {
                                                if (listSalesType.Count == 1)
                                                {
                                                    <option value="@listSalesType.FirstOrDefault().Code">@listSalesType.FirstOrDefault().Description</option>
                                                }
                                                else
                                                {
                                                    <option value="" selected>Tất cả</option>
                                                    foreach (var item in listSalesType)
                                                    {
                                                        <option value="@item.Code">@item.Description</option>
                                                    }
                                                }
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                        <label class="font-weight-bold" style="font-weight:bold !important;">Từ khóa</label>
                                        <div class="form-group">
                                            <input type="text" class="form-control" id="txt-text-search" name="TextSearch" placeholder="Số đơn hàng / barcode / mã sản phẩm">
                                        </div>
                                </div>

                                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12" style="margin-top:25px;">
                                        <button type="button" id="btn-search" class="btn btn-primary"><i class="fa fa-search"></i>Tìm</button>
                                        <button type="button" id="btn-export-excel" class="btn btn-success"><i class="fa fa-file-excel"></i>Xuất Excel</button>
                                </div>

                                </div>
                                <input type="hidden" id="hdFromDate" name="FromDate" />
                                <input type="hidden" id="hdToDate" name="ToDate" />
                                <input type="hidden" id="hd-set-server-ip" name="SetServer" />
                        </div>
                    </div>
                </div>
            </div>
        }

        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xl-12">
                <div class="card card-custom bgi-no-repeat gutter-b card-stretch">
                    <div class="card-body">
                        <table id="tbl-report-sales-detail-promotion" class="table table-bordered table-hover text-nowrap">
                            <thead>
                                <tr class="text-center" style="text-transform:uppercase;">
                                    <th>STT</th>
                                    <th>Ngày/Tháng</th>
                                    <th>ST/CH</th>
                                    <th>Máy POS</th>
                                    <th>Số đơn hàng</th>
                                    <th>Hình thức bán hàng</th>
                                    <th>Loại đơn hàng</th>
                                    <th>Số đơn hàng gốc</th>
                                    <th>Barcode</th>
                                    <th>Mã sản phẩm</th>
                                    <th>Tên sản phẩm</th>
                                    <th>CTKM/Voucher</th>
                                    <th>ĐVT</th>
                                    <th>Số lượng</th>
                                    <th>Giá bán</th>
                                    <th>Giảm giá</th>
                                    <th>Thành tiền trước thuế (VAT)</th>
                                    <th>VAT(%)</th>
                                    <th>Tiền thuế VAT</th>
                                    <th>Thành tiền sau thuế (VAT)</th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>


<input type="hidden" id="temp-from-date-check-31" />
<input type="hidden" id="temp-to-date-check-31" />

@section Scripts
{
    <script type="text/javascript">
        var table;
        function LoadComboxStoreByServer(ipServer) {
            $("#sel-store-no").empty();
            if (ipServer == null || ipServer == '') {
                var html = "<option value=\"\" selected>Tất cả</option>";
                return $('#sel-store-no').html(html);
            }
            var fromData = new FormData();
            fromData.append('IPServer', ipServer);
            loading.showloading();
            $.ajax({
                type: "POST",
                url: '@Url.Action("LoadStoreBySetServerIP", "Common")',
                data: fromData,
                contentType: "application/json",
                dataType: "json",
                contentType: false,
                processData: false,
                success: function (data) {
                    loading.hideloading();
                    if (data != null) {
                        if (data.length == 1) {
                            var html = "<option value='" + data[0].SiteNo + "'>" + data[0].DisplayComboSite + "</option>";
                            $('#sel-store-no').html(html);
                        }
                        else if (data.length > 1) {
                            var html = "<option value=\"\" selected>Tất cả</option>";
                            data.forEach((name, i) => {
                                if (name.SiteNo != "") {
                                    html += "<option value='" + name.SiteNo + "'>" + name.DisplayComboSite + "</option>";
                                }
                            });
                            $('#sel-store-no').html(html);
                        }
                    }
                },
                error: function ()
                {
                    toastr.error('Lỗi hệ thống');
                }
            });
        }

        function getFromDate() {
            //debugger
            var result = "";
            var fromDate = $('#txt-from-date').val();
            if (fromDate != null && fromDate != "") {
                result = fromDate.split('/')[2] + "/" + fromDate.split('/')[1] + "/" + fromDate.split('/')[0];
            }
            else {
                result = "";
            }
            return result;
        }

        function getToDate() {
            var result = "";
            var toDate = $('#txt-to-date').val();
            if (toDate != null && toDate != "") {
                result = toDate.split('/')[2] + "/" + toDate.split('/')[1] + "/" + toDate.split('/')[0];
            }
            else {
                result = "";
            }
            return result;
        }

        $('#txt-return-order').keypress(function (e) {
            var key = e.which;
            if (key == 13) {
                $('button[name = btnSearch]').click();
                return false;
            }
        });

        $('#txt-text-search').keypress(function (e) {
            var key = e.which;
            if (key == 13) {
                $('button[name = btnSearch]').click();
                return false;
            }
        });

        function getFromDateByCheck31(fromDateStr) {
            $('#fromDateStr').val(moment().format('DD/MM/YYYY'));

            $('#fromDateStr').daterangepicker({
                singleDatePicker: true,
                locale: {
                    format: 'DD/MM/YYYY'
                }
            });

            var dateStr = $('#fromDateStr').val();
            return dateStr;
        }

        function getToDateByCheck31(toDateStr) {
            $('#toDateStr').val(moment().format('DD/MM/YYYY'));

            $('#toDateStr').daterangepicker({
                singleDatePicker: true,
                locale: {
                    format: 'DD/MM/YYYY'
                }
            });

            var dateStr = $('#toDateStr').val();
            return dateStr;
        }

        $(document).ready(function () {

            $('.select2').select2();

            $('#txt-from-date').val(moment().format('DD/MM/YYYY'));
            $('#txt-from-date').datepicker({
                format: 'dd/mm/yyyy',
                autoUpdateInput: false,
                todayHighlight: true,
                singleDatePicker: true,
                autoclose: true
            });

            $('#txt-to-date').val(moment().format('DD/MM/YYYY'));
            $('#txt-to-date').datepicker({
                format: 'dd/mm/yyyy',
                autoUpdateInput: false,
                todayHighlight: true,
                singleDatePicker: true,
                autoclose: true
            });

            LoadComboxStoreByServer($("#sel-set-server").val());
            $("#sel-set-server").on("change", function (e) {
                LoadComboxStoreByServer($("#sel-set-server").val());
            });

            table = $('#tbl-report-sales-detail-promotion').DataTable({
                destroy: true,
                deferLoading: 0,
                select: {
                    style: 'single'
                },

                dom: '<"top"iflp<"clear">>rt',
                paging: true,
                pagingType: "simple_numbers",
                responsive: false,
                processing: false,          // for show progress bar
                serverSide: true,           // for process server side
                filter: false,              // this is for disable filter (search box)
                orderMulti: false,          // for disable multiple column at once
                autoWidth: false,
                ordering: false,
                displayLength: 15,
                searching: false,
                lengthChange: false,
                scrollCollapse: false,
                scrollX: true,

                ajax: {
                    url: '@Url.Action("GetSalesDetailPromotion", "Report")',
                    type: "POST",
                    datatype: "json",
                    data: function (d) {
                        d.fromDate = getFromDate();
                        d.toDate = getToDate();
                        d.setServer = $('#sel-set-server').val();
                        d.serverRead = $("#sel-set-server").select2().find(":selected").data("serverread");
                        d.storeNo = $('#sel-store-no').val();
                        d.orderType = $('#sel-order-type').val();
                        d.salesType = $('#sel-sales-type').val();
                        d.textSearch = $('#txt-text-search').val();
                    }
                },

                columns: [
                    {
                        data: "ID",
                        className: 'text-center',
                        render: function (data, type, row, meta) {
                            var start = meta.settings._iDisplayStart;
                            return meta.row + 1 + start;
                        }
                    },

                    {
                        data: "OrderDateStr",    // Ngày kinh doanh
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OrderDateStr == null || row.OrderDateStr == "") {
                                html += '<span style="font-weight:700; display: block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700; display: block;">' + row.OrderDateStr + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "StoreNo",
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.StoreNo == null || row.StoreNo == "") {
                                html += '<span style="font-weight:400; display: block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400; display: block;">' + row.StoreNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "POSTerminalNo",
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.POSTerminalNo == null || row.POSTerminalNo == "") {
                                html += '<span style="font-weight:400; display: block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400; display: block;">' + row.POSTerminalNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "OrderNo",
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OrderNo == null || row.OrderNo == "") {
                                html += '<span style="font-weight:700; display: block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700; display: block;">' + row.OrderNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "SalesTypeName",   // Hinh thuc ban hang
                        render: function (data, type, row) {
                            var html = "";
                            if (row.SalesTypeName == null || row.SalesTypeName == "") {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + row.SalesTypeName + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "OrderType",    // Loại đơn hàng
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OrderType == "Đơn hàng trả") {
                                html += '<span style="display: block; text-align:left; font-weight:700; color: #db0b0b;">' + row.OrderType + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:left;">' + row.OrderType + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "OrigOrder",    // Đơn hàng gốc
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OrigOrder == null || row.OrigOrder == "") {
                                html += '<span style="font-weight:400; display: block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400; display: block;">' + row.OrigOrder + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "Barcode",     // Mã Barcode
                        render: function (data, type, row) {
                            var html = "";
                            if (row.Barcode == null || row.Barcode == "") {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + row.Barcode + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "ItemNo",  // Mã sản phẩm
                        render: function (data, type, row) {
                            var html = "";
                            if (row.ItemNo == null || row.ItemNo == "") {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + row.ItemNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "Description",   // Tên sản phẩm
                        render: function (data, type, row) {
                            var html = "";
                            if (row.Description == null || row.Description == "") {
                                html += '<span style="display:block;text-align:left;font-weight:400;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:left;font-weight:400;">' + row.Description + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "PromotionNo",   // Mã CTKM  hoặc Mã Voucher
                        render: function (data, type, row) {
                            var html = "";
                            if (row.PromotionNo == null || row.PromotionNo == "") {
                                html += '<span style="display: block; text-align:center; font-weight:700;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:center; font-weight:700;">' + row.PromotionNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "UnitOfMeasure",
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.UnitOfMeasure == null || row.UnitOfMeasure == "") {
                                html += '<span style="display:block;font-weight:400;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;font-weight:400;">' + row.UnitOfMeasure + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "Quantity",
                        className: "text-center",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.Quantity == null || row.Quantity == "") {
                                html += '<span style="display: block;">' + "" + '</span>';
                            }
                            else
                            {
                               html += '<span style="display: block;">' + parseFloat(row.Quantity).toLocaleString('en') + '</span>';  
                            }
                            return html;
                        }
                    },

                    {
                        data: "UnitPrice",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.UnitPrice == null || row.UnitPrice == "") {
                                html += '<span style="display: block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display: block;">' + parseFloat(row.UnitPrice).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "DiscountAmount",  // Giảm giá
                        render: function (data, type, row) {
                            var html = "";
                            if (row.DiscountAmount == null || row.DiscountAmount == "") {
                                html += '<span style="display: block; text-align:right;font-weight:700;color: #db0b0b;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:right;font-weight:700;color: #db0b0b;">' + parseFloat(row.DiscountAmount).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "LineAmountBeforeVAT",  // Thành tiền trước thuế
                        render: function (data, type, row) {
                            var html = "";
                            if (row.LineAmountBeforeVAT == null || row.LineAmountBeforeVAT == "") {
                                html += '<span style="display: block; text-align:right;font-weight:700;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:right;font-weight:700;">' + parseFloat(row.LineAmountBeforeVAT).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "VATCode",        // Thuế suất GTGT (%)
                        render: function (data, type, row) {
                            var html = "";
                            if (row.VATCode == null || row.VATCode == "") {
                                html += '<span style="display: block; text-align:center;">' + "" + '</span>';
                            }
                            else if (row.VATCode == 0) {
                                html += '<span style="display: block; text-align:center;">' + row.VATCode + "%" + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:center;">' + ((row.VATCode == 1) ? "Non Tax" : row.VATCode + "%") + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "VATAmount",          // Tiền thuế VAT
                        render: function (data, type, row) {
                            var html = "";
                            if (row.VATAmount == null || row.VATAmount == "") {
                                html += '<span style="display: block; text-align:right;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:right;">' + parseFloat(row.VATAmount).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "LineAmountIncVAT",   // Thành tiền sau thuế
                        render: function (data, type, row) {
                            var html = "";
                            if (row.LineAmountIncVAT == null || row.LineAmountIncVAT == "") {
                                html += '<span style="display: block; text-align:right;font-weight:700;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display: block; text-align:right;font-weight:700;">' + parseFloat(row.LineAmountIncVAT).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    }
                ]

            }).on('processing.dt', function (e, settings, processing) {
                if (processing) {
                    loading.showloading();
                } else {
                    loading.hideloading();
                }
            });

            $('#tbl-report-sales-detail-promotion tbody').on('click', 'tr', function () {
                $(this).toggleClass('selected');
            });

            $("#btn-search").on("click", function (e) {
                var _fromDate = getFromDate();
                var _toDate = getToDate();
                if (_fromDate > _toDate) {
                    toastr.error('Từ ngày <= đến ngày. Vui lòng kiểm tra lại');
                    return;
                }

                var fromDateStr = getFromDate();  //yyyy/MM/dd
                var toDateStr = getToDate();      //yyyy/MM/dd

                var yearStrFrom = fromDateStr.split('/')[0];  //yyyy
                var monthStrFrom = fromDateStr.split('/')[1]; //MM
                var dayStrFrom = fromDateStr.split('/')[2];   //dd
                var _dateFrom = dayStrFrom.substr(0, 2);

                var yearStrTo = toDateStr.split('/')[0];  //yyyy
                var monthStrTo = toDateStr.split('/')[1]; //MM
                var dayStrTo = toDateStr.split('/')[2];   //dd
                var _dateTo = dayStrTo.substr(0, 2);

                $('#fromDate').val(moment().format('DD/MM/YYYY'));
                $('#fromDate').daterangepicker({
                    singleDatePicker: true,
                    locale: {
                        format: 'DD/MM/YYYY'
                    }
                });

                $('#toDate').val(moment().format('DD/MM/YYYY'));
                $('#toDate').daterangepicker({
                    singleDatePicker: true,
                    locale: {
                        format: 'DD/MM/YYYY'
                    }
                });

                var fromDate = _dateFrom + "/" + monthStrFrom + "/" + yearStrFrom;
                var toDate = _dateTo + "/" + monthStrTo + "/" + yearStrTo;

                $("#temp-from-date-check-31").val(fromDate);
                $("#temp-to-date-check-31").val(toDate);

                var _fromDate = $("#temp-from-date-check-31").val().split('/');
                var _toDate = $("#temp-to-date-check-31").val().split('/');

                var newFromDate = new Date(_fromDate[1] + '/' + _fromDate[0] + '/' + _fromDate[2]);
                var newToDate = new Date(_toDate[1] + '/' + _toDate[0] + '/' + _toDate[2]);

                var Difference_In_Time = newToDate.getTime() - newFromDate.getTime();
                var Difference_In_Days = Difference_In_Time / (1000 * 3600 * 24);

                if (Difference_In_Days > 31) {
                    toastr.error("Do dữ liệu lớn, vui lòng chọn thời gian tối đa 31 ngày");
                    loading.hideloading();
                    return;
                }

                var checkStoreBySet = $('#sel-store-no').val();
                if (checkStoreBySet == "-1") {
                    toastr.error('Bạn không có quyền xem thông tin trong vùng set này');
                    loading.hideloading();
                    return;
                }
                table.draw();
            });

            $("#btn-export-excel").on("click", function (e) {

                var _fromDate = getFromDate();
                var _toDate = getToDate();
                if (_fromDate > _toDate) {
                    toastr.error('Từ ngày phải <= đến ngày. Vui lòng kiểm tra lại');
                    return;
                }

                var fromDateStr = getFromDate();  //yyyy/MM/dd
                var toDateStr = getToDate();      //yyyy/MM/dd

                var yearStrFrom = fromDateStr.split('/')[0];  //yyyy
                var monthStrFrom = fromDateStr.split('/')[1]; //MM
                var dayStrFrom = fromDateStr.split('/')[2];   //dd
                var _dateFrom = dayStrFrom.substr(0, 2);

                var yearStrTo = toDateStr.split('/')[0];  //yyyy
                var monthStrTo = toDateStr.split('/')[1]; //MM
                var dayStrTo = toDateStr.split('/')[2];   //dd
                var _dateTo = dayStrTo.substr(0, 2);

                $('#fromDate').val(moment().format('DD/MM/YYYY'));
                $('#fromDate').daterangepicker({
                    singleDatePicker: true,
                    locale: {
                        format: 'DD/MM/YYYY'
                    }
                });

                $('#toDate').val(moment().format('DD/MM/YYYY'));
                $('#toDate').daterangepicker({
                    singleDatePicker: true,
                    locale: {
                        format: 'DD/MM/YYYY'
                    }
                });

                var fromDate = _dateFrom + "/" + monthStrFrom + "/" + yearStrFrom;
                var toDate = _dateTo + "/" + monthStrTo + "/" + yearStrTo;

                $("#temp-from-date-check-31").val(fromDate);
                $("#temp-to-date-check-31").val(toDate);

                var _fromDate = $("#temp-from-date-check-31").val().split('/');
                var _toDate = $("#temp-to-date-check-31").val().split('/');

                var newFromDate = new Date(_fromDate[1] + '/' + _fromDate[0] + '/' + _fromDate[2]);
                var newToDate = new Date(_toDate[1] + '/' + _toDate[0] + '/' + _toDate[2]);

                var Difference_In_Time = newToDate.getTime() - newFromDate.getTime();
                var Difference_In_Days = Difference_In_Time / (1000 * 3600 * 24);

                if (Difference_In_Days > 31) {
                    toastr.error("Do dữ liệu lớn, vui lòng chọn thời gian tối đa 31 ngày");
                    loading.hideloading();
                    return;
                }

                var checkStoreBySet = $('#sel-store-no').val();
                if (checkStoreBySet == "-1") {
                    toastr.error('Bạn không có quyền xem thông tin trong vùng set này');
                    loading.hideloading();
                    return;
                }

                $('#hdFromDate').val(_fromDate);
                $('#hdToDate').val(_toDate);

                var ipServerSet = $("#sel-set-server").select2().find(":selected").data("serverread");
                $('#hd-set-server-ip').val(ipServerSet);

                $('#frm-export-excel').submit();

            });

        });

    </script>
}

