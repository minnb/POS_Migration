@{
    Layout = null;
}

@using VCM.BLUEPOS.Model.Report;
@model ExportPDFRevenueOrderSalesModel

<!DOCTYPE html>
<html lang="en" manifest="">
<head>
    <title>Doanh thu theo nhân viên</title>
    @Styles.Render("~/Content/css")
    
    <style>
        table > tbody > tr {
            line-height: 10px !important;
        } 
    </style>
    @{
    }
</head>
<body>


    <section class="content">
        <div class="container-fluid">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 mb-1" style="font-family:Arial !important">

                <!-- title row -->
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <span class="float-left">
                            ID của người dùng: @Model.UserName
                            <br />
                            Tên người dùng: @Model.FullName
                        </span>
                        <span style="float:right;">
                            Ngày giờ: @DateTime.Now.ToString("dd/MM/yyyy HH:mm:ss")
                            <br />@*Trang 1/1*@
                        </span>
                    </div>
                    <!-- /.col -->
                </div>
                <!-- info row -->
                <div class="row" style="clear:both;padding-top:20px;">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">

                        <h3 class="text-center text-uppercase" style="font-size:20px;font-weight:bold;margin-bottom:20px;">Tổng hợp doanh thu theo nhân viên</h3>
                        <span class="float-left">
                            <strong>Cửa hàng:</strong> @Model.StoreName <br />
                            <strong>
                                Ngày giao dịch:
                            </strong> @Model.TransDate
                        </span>

                    </div>
                </div>

                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <table class="table table-bordered">
                            <thead>
                                <tr style="text-align:center;">
                                    <th>STT</th>
                                    <th>Mã nhân viên</th>
                                    <th>Tên nhân viên</th>
                                    <th style="text-align:right">Doanh thu</th>

                                </tr>
                            </thead>
                            <tbody>
                                @{ if (Model.lstSaleEmp.Count > 0)
                                    {
                                        var stt = 1;
                                        var total = 0.0;
                                        foreach (var item in Model.lstSaleEmp)
                                        {
                                            <tr>
                                                <td style="text-align:center">
                                                    @stt
                                                </td>
                                                <td style="text-align:left">
                                                    @item.StaffCode
                                                </td>
                                                <td style="text-align:left"> @item.FullName</td>
                                                <td style="text-align:right">@string.Format("{0:###,###}", item.AmountTotal)</td>
                                            </tr>
                                            stt++;
                                            total += (double)item.AmountTotal;
                                        }
                                        <tr style="font-weight:bold;font-size:16px;background-color:#cecaca">
                                            <td colspan="3">Total</td>
                                            <td style="text-align:right">@string.Format("{0:###,###}", total)</td>
                                        </tr>
                                    }
                                }

                            </tbody>
                        </table>
                    </div>
                </div>


            </div>
        </div>

    </section>

</body>
</html>

