@using VCM.BLUEPOS.Model.CrownX;
@using VCM.BLUEPOS.Model.OptionModel;
@using VCM.BLUEPOS.Model.Common;

@{
    ViewBag.Title = "Báo cáo đơn hàng khuyến mãi Masaner";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var listStore = ViewBag.ListStore as List<ArraySiteNoModel>;
    var listSetDB = ViewBag.SetDB as List<SQLServerModel>;
    var listOrderType = ViewBag.ListOrderType as List<OptionDataModel>;
}

<div class="subheader py-2 py-lg-6 subheader-transparent" id="kt_subheader">
    <div class="container d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
        <div class="d-flex align-items-center flex-wrap mr-1">
            <div class="d-flex align-items-baseline flex-wrap mr-5">
                <ul class="breadcrumb breadcrumb-transparent breadcrumb-dot font-weight-bold p-0 my-2 font-size-lg">
                    <li class="breadcrumb-item text-muted">
                        <a style="color:#0C713D;">Vận hành</a>
                    </li>
                    <li class="breadcrumb-item text-muted">
                        <a style="color:#0C713D;">CrownX</a>
                    </li>
                    <li class="breadcrumb-item text-muted">
                        <a style="color:#0C713D;">Báo cáo đơn hàng khuyến mãi Masaner</a>
                    </li>
                </ul>
            </div>
        </div>

        <div class="d-flex align-items-center">
            <div id="title-site" style="color:#1144b6;font-weight:bold;"></div>
        </div>
    </div>
</div>


<div class="d-flex flex-column-fluid">
    <div class="container">
        @using (Html.BeginForm("ExportExcel_ReportOfferStaffTransaction", "CrownX", FormMethod.Get, new { @id = "frm-export-excel" }))
        {
            <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xl-12">
                    <div class="card card-custom bgi-no-repeat gutter-b card-stretch">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Từ ngày</label>
                                    <div class="form-group">
                                        <div class="input-group">
                                            <div class="input-group-prepend">
                                                <label class="input-group-btn" for="txt-from-date">
                                                    <span class="btn btn-default">
                                                        <span class="glyphicon glyphicon-calendar">
                                                            <i class="far fa-calendar-alt"></i>
                                                        </span>
                                                    </span>
                                                </label>
                                            </div>
                                            <input type="text" class="form-control float-right" id="txt-from-date" name="TxtFromDate" placeholder="Từ ngày">
                                        </div>
                                    </div>
                                </div>

                                <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Đến ngày</label>
                                    <div class="form-group">
                                        <div class="input-group">
                                            <div class="input-group-prepend">
                                                <label class="input-group-btn" for="txt-to-date">
                                                    <span class="btn btn-default">
                                                        <span class="glyphicon glyphicon-calendar">
                                                            <i class="far fa-calendar-alt"></i>
                                                        </span>
                                                    </span>
                                                </label>
                                            </div>
                                            <input type="text" class="form-control float-right" id="txt-to-date" name="TxtToDate" placeholder="Đến ngày">
                                        </div>
                                    </div>
                                </div>

                                <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important">Vùng Set<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="input-group">
                                        <select class="form-control select2" style="width:100%;" id="set-server" name="SetServer">
                                            @if (listSetDB != null && listSetDB.Count > 0)
                                            {
                                                if (listSetDB.Count == 1)
                                                {
                                                    <option value="@listSetDB.FirstOrDefault().IPServer" data-serverread="@listSetDB.FirstOrDefault().IPServerRead">@listSetDB.FirstOrDefault().Description</option>
                                                }
                                                else
                                                {
                                                    foreach (var item in listSetDB)
                                                    {
                                                        <option value="@item.IPServer" data-serverread="@item.IPServerRead">@item.Description</option>
                                                    }
                                                }
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="col-lg-3 col-md-3 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Cửa hàng<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="input-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-store-no" name="StoreNo"></select>
                                    </div>
                                </div>

                                <div class="col-lg-3 col-md-3 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Máy POS<strong style="color:#ff0000"> (*)</strong></label>
                                    <div class="form-group">
                                        <select class="form-control select2" style="width: 100%;" id="sel-pos-id" name="PosTerminal"></select>
                                    </div>
                                </div>

                                <div class="col-lg-2 col-md-2 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important">Loại đơn hàng</label>
                                    <div class="input-group">
                                        <select class="form-control select2" style="width:100%;" id="sel-order-type" name="OrderType">
                                            <option value="">Tất cả</option>
                                            <option value="1">Đơn hàng bán</option> <!-- TransactionType = 1 -->
                                            <option value="2">Đơn hàng trả</option> <!-- TransactionType = 2 -->
                                        </select>
                                    </div>
                                </div>

                                <div class="col-lg-4 col-lg-4 col-sm-12 col-xs-12">
                                    <label class="font-weight-bold" style="font-weight:bold !important;">Từ khóa</label>
                                    <div class="form-group">
                                        <input type="text" class="form-control" id="txt-text-search" name="TextSearch" placeholder="Nhập số thẻ khách hàng / số đơn hàng">
                                    </div>
                                </div>

                                <input type="hidden" id="hdFromDate" name="FromDate" />
                                <input type="hidden" id="hdToDate" name="ToDate" />

                                <div class="col-lg-6 col-md-6 col-sm-12 col-xs-12" style="margin-top:25px;">
                                    <button type="button" class="btn btn-primary" id="btn-search"><i class="fa fa-search"></i>Tìm</button>
                                    <button type="button" class="btn btn-success btn-export-excel" id="btn-export-excel"><i class="fa fa-file-excel"></i>Xuất Excel</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }

        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xl-12">
                <div class="card card-custom bgi-no-repeat gutter-b card-stretch">
                    <div class="card-body">
                        <table id="tbl-report-staff-member-remn-mansan-list" class="table table-bordered table-hover">
                            <thead>
                                <tr class="text-center" style="text-transform:uppercase;">
                                    <th>#</th>
                                    <th>STT</th>
                                    <th>Ngày kinh doanh</th>
                                    <th>Thời gian</th>
                                    <th>Cửa hàng</th>
                                    <th>Máy POS</th>
                                    <th>Số đơn hàng</th>
                                    <th>Loại đơn hàng</th>
                                    <th>Số đơn hàng gốc</th>
                                    <th>Tổng số tiền</th>
                                    @*<th>Hạn mức đã sử dụng</th>
                                    <th>Hạn mức</th>*@
                                    <th>Mã nhân viên</th>
                                    <th>Số thẻ khách hàng</th>
                                    <th>Hội viên</th>
                                    <th>Thu ngân</th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>

<div class="modal fade" tabindex="-1" role="dialog" aria-hidden="true" id="modal-detail-order-list">
    <div class="modal-dialog modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal-title-detail-order">Chi tiết đơn hàng</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <i aria-hidden="true" class="ki ki-close"></i>
                </button>
            </div>
            <div class="modal-body" id="modal-detail-order-by-offer-staff-transaction-body">
                <div class="card">
                    <div class="card-body table-responsive">
                        <table class="table table-bordered table-hover text-nowrap" id="tbl-detail-order-by-offer-staff-transaction">
                            <thead>
                                <tr class="text-center" style="text-transform:uppercase">
                                    <th style="display:none;">LineNo</th>
                                    <th>Barcode</th>
                                    <th>Mã sản phẩm</th>
                                    <th>Tên sản phẩm</th>
                                    <th>ĐVT</th>
                                    <th>Số lượng</th>
                                    <th>Giá bán</th>
                                    <th>Giảm giá</th>
                                    <th>Tiền thuế (VAT)</th>
                                    <th>Thành tiền sau thuế (VAT)</th>
                                    <th>Mã CTKM/Voucher</th>                                    
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </div>
            </div>

            <input type="hidden" id="view-detail-store-no-temp" name="ViewStoreNo">
            <input type="hidden" id="view-detail-order-no-temp" name="ViewOrderNo">
            <input type="hidden" id="view-detail-order-type-return-temp" name="ViewOrderTypeReturn">

            <div class="modal-footer">
                <button type="button" id="close-modal-detail-order-list" class="btn btn-light-danger font-weight-bold" data-dismiss="modal"><i class="la la-close"></i>Đóng</button>
            </div>
        </div>
    </div>
</div>



    @section Scripts
{
        <script type="text/javascript">

        function LoadComboxStoreByServer(ipServer) {
            loading.showloading();
            var fromData = new FormData();
            fromData.append('IPServer', ipServer);
            $.ajax({
                type: "POST",
                url: '@Url.Action("LoadStoreBySetServerIP", "Common")',
                data: fromData,
                contentType: "application/json",
                dataType: "json",
                contentType: false,
                processData: false,
                success: function (data) {
                    if (data != null) {
                        if (data.length == 1) {
                            var html = "<option value='" + data[0].SiteNo + "'>" + data[0].DisplayComboSite + "</option>";
                            $('#sel-store-no').html(html);
                            LoadComboxPOSTerminalByStore(data[0].SiteNo);
                        }
                        else if (data.length > 1) {
                            var html = "<option value=\"\" selected>Tất cả</option>";
                            data.forEach((name, i) => {
                                if (name.SiteNo != "") {
                                    html += "<option value='" + name.SiteNo + "'>" + name.DisplayComboSite + "</option>";
                                }
                            });
                            $('#sel-store-no').html(html);
                        }
                    }
                    loading.hideloading();
                },
                error: function ()
                {
                    loading.hideloading();
                    toastr.error('Lỗi hệ thống');
                }
            });
        };

    function LoadComboxPOSTerminalByStore(storeNo) {
            loading.showloading();
            if (storeNo == null || storeNo == '') {
                var html = "<option value=\"\" selected>Tất cả</option>";
                return $('#sel-pos-id').html(html);
            }
            $.ajax({
                type: "POST",
                url: '@Url.Action("LoadPOSTerminalByStore", "Common")',
                data: { StoreNo: storeNo },
                dataType: "json",
                success: function (data) {
                    if (data != null) {
                        if (data.length == 1) {
                            var html = "<option value='" + data[0].PosID + "'>" + data[0].PosID + "</option>";
                            $('#sel-pos-id').html(html);
                        }
                        else if (data.length > 1) {
                            var html = "<option value=\"\" selected>Tất cả</option>";
                            data.forEach((name, i) => {
                                if (name.PosID != "") {
                                    html += "<option value='" + name.PosID + "'>" + name.PosID + "</option>";
                                }
                            });
                            $('#sel-pos-id').html(html);
                        }
                    }
                    loading.hideloading();
                },
                error: function ()
                {
                    loading.hideloading();
                    toastr.error('Lỗi hệ thống');
                }
          });
        };

        function getFromDate() {
            var result = "";
            var fromDate = $('#txt-from-date').val();
            if (fromDate != null && fromDate != "") {
                //result = fromDate.split('/')[0] + "/" + fromDate.split('/')[1] + "/" + fromDate.split('/')[2];
                result = fromDate.split('/')[2] + "/" + fromDate.split('/')[1] + "/" + fromDate.split('/')[0];
            }
            else {
                result = "";
            }
            return result;
        };

        function getToDate() {
            var result = "";
            var toDate = $('#txt-to-date').val();
            if (toDate != null && toDate != "") {
                //result = toDate.split('/')[0] + "/" + toDate.split('/')[1] + "/" + toDate.split('/')[2];
                result = toDate.split('/')[2] + "/" + toDate.split('/')[1] + "/" + toDate.split('/')[0];
            }
            else {
                result = "";
            }
            return result;
        };


    $(document).ready(function () {

        var table;
        var table_detail_order_list;

        $('.select2').select2();

        $('#txt-from-date').val(moment().format('DD/MM/YYYY'));
        $('#txt-from-date').datepicker(
            {
                format: 'dd/mm/yyyy',
                autoUpdateInput: false,
                todayHighlight: true,
                singleDatePicker: true,
                autoclose: true
            });

        $('#txt-to-date').val(moment().format('DD/MM/YYYY'));
        $('#txt-to-date').datepicker(
        {
            format: 'dd/mm/yyyy',
            autoUpdateInput: false,
            todayHighlight: true,
            singleDatePicker: true,
            autoclose: true
        });

        LoadComboxStoreByServer($("#set-server").val());
        $("#set-server").on("change", function (e) {
            LoadComboxStoreByServer($("#set-server").val());
        });

        LoadComboxPOSTerminalByStore($("#sel-store-no").val());
        $("#sel-store-no").on('change', function (e) {
            LoadComboxPOSTerminalByStore($("#sel-store-no").val());
        });

        table = $('#tbl-report-staff-member-remn-mansan-list').DataTable({
                destroy: true,
                deferLoading: 0,
                select: {
                    style: 'single'
            },
            drawCallback: function (settings) {
                $(".btn-detail-order-list").click(function () {
                    //debugger
                    var _storeNo = $(this).attr("data-store-no");
                    $('#view-detail-store-no-temp').val(_storeNo);

                    var _orderNo = $(this).attr("data-order-no");
                    $('#view-detail-order-no-temp').val(_orderNo);

                    var _ordertype = $(this).attr("data-order-type");
                    var orderType = "";
                    if (_ordertype == "Đơn trả") {
                        orderType = 1;    // true
                    }
                    else {
                        orderType = "2";   // false
                    }
                    $('#view-detail-order-type-return-temp').val(orderType);

                    table_detail_order_list.draw();

                    $('#modal-detail-order-list').modal({
                        backdrop: 'static',
                        keyboard: true
                    });

                });
            },
                dom: '<"top"iflp<"clear">>rt',
                paging: true,
                pagingType: "simple_numbers",
                responsive: false,
                processing: false,          // for show progress bar
                serverSide: true,           // for process server side
                filter: false,              // this is for disable filter (search box)
                orderMulti: false,          // for disable multiple column at once
                info: true,                 // Hiển thị dòng _START_ đến dòng _END_ của tổng số _TOTAL_ dòng,
                autoWidth: false,
                ordering: false,
                displayLength: 15,
                searching: false,
                lengthChange: false,

                ajax: {
                    url: '@Url.Action("GetReportStaffMemberRemnList", "CrownX")',
                    type: "POST",
                    datatype: "json",
                    data: function (d) {
                        d.FromDate = getFromDate();
                        d.ToDate = getToDate();
                        d.SetServer = $('#set-server').val();
                        d.ServerRead = $("#set-server").select2().find(":selected").data("serverread");
                        d.StoreNo = $("#sel-store-no").val();
                        d.PosTerminal = $("#sel-pos-id").val();
                        d.OrderType = $("#sel-order-type").val();
                        d.TextSearch = $("#txt-text-search").val();
                    }
                },
            columns: [

                    {
                        className: 'text-center',
                        render: function (data, type, row) {
                            //debugger
                            var html = '<a type="button" class="btn btn-icon btn-primary btn-detail-order-list" data-store-no ="' + row.StoreNo + '"  data-order-no ="' + row.OrderNo + '"  data-order-type ="' + row.OrderType + '"  data-toggle="tooltip" data-placement="left" title="Xem chi tiết"  style="margin-left: 3px; color:#fff;text-align:center;"><i class="fa fa-list"></i><span class="pulse-ring"></span></a>';
                            return html;
                        }
                    },

                    {
                        data: "ID",
                        className: "text-center",
                        render: function (data, type, row, meta) {
                            var start = meta.settings._iDisplayStart;
                            return meta.row + 1 + start;
                        }
                    },

                    {
                        data: "CrtDateStr", // Ngày kinh doanh
                        className: 'text-center',
                        render: function (data, type, row) {
                            var html = "";
                            if (row.CrtDateStr == null || row.CrtDateStr == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:center;">' + row.CrtDateStr + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "TimeDateStr", // Thời gian
                        className: 'text-center',
                        render: function (data, type, row) {
                            var html = "";
                            if (row.TimeDateStr == null || row.TimeDateStr == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:center;">' + row.TimeDateStr + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "StoreNo",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.StoreNo == null || row.StoreNo == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.StoreNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "PosNo",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.PosNo == null || row.PosNo == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.PosNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "OrderNo",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OrderNo == null || row.OrderNo == "") {
                                html += '<span style="display:block;text-align:center;font-weight:700;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:center;font-weight:700;">' + row.OrderNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "OrderType",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OrderType == null || row.OrderType == "") {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + "" + '</span>';
                            }
                            else if (row.OrderType == "Đơn hàng trả") {
                                html += '<span style="display:block;text-align:center;font-weight:700;color:red;">' + row.OrderType + '</span>';
                            } else {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + row.OrderType + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "ReturnedOrderNo",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.ReturnedOrderNo == null || row.ReturnedOrderNo == "") {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:center;font-weight:400;">' + row.ReturnedOrderNo + '</span>';
                            }
                            return html;
                        }
                    },
                   

                    {
                        data: "AmountInclVAT",  // Tông số tiền
                        className: 'text-center',
                        render: function (data, type, row) {
                            var html = "";
                            if (row.AmountInclVAT == null || row.AmountInclVAT == "") {
                                html += '<span style="font-weight:400;display:block;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;">' + parseFloat(row.AmountInclVAT).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    //{
                    //    data: "Amount",  // Han muc da su dung
                    //    className: 'text-center',
                    //    render: function (data, type, row) {
                    //        var html = "";
                    //        if (row.Amount == null || row.Amount == "") {
                    //            html += '<span style="font-weight:400;display:block;">' + "" + '</span>';
                    //        }
                    //        else {
                    //            html += '<span style="font-weight:700;display:block;">' + parseFloat(row.Amount).toLocaleString('en') + '</span>';
                    //        }
                    //        return html;
                    //    }
                    //},

                    //{
                    //    data: "InitQuota",  // Han muc
                    //    render: function (data, type, row) {
                    //        var html = "";
                    //        if (row.InitQuota == null || row.InitQuota == "") {
                    //            html += '<span style="display:block;text-align:center;">' + "" + '</span>';
                    //        }
                    //        else {
                    //            html += '<span style="font-weight:700;display:block;text-align:center;color:green;">' + parseFloat(row.InitQuota).toLocaleString('en') + '</span>';
                    //        }
                    //        return html;
                    //    }
                    //},

                    {
                        data: "StaffCode",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.StaffCode == null || row.StaffCode == "") {
                                html += '<span style="display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.StaffCode + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "PhoneNumber",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.PhoneNumber == null || row.PhoneNumber == "") {
                                html += '<span style="display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:center;">' + row.PhoneNumber + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "ClubCode",  // Hội viên
                        render: function (data, type, row) {
                            var html = "";
                            if (row.ClubCode == null || row.ClubCode == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.ClubCode + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "CashierID",  // Thu ngân
                        render: function (data, type, row) {
                            var html = "";
                            if (row.CashierID == null || row.CashierID == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.CashierID + '</span>';
                            }
                            return html;
                        }
                    }
            ]

            }).on('processing.dt', function (e, settings, processing) {
                if (processing) {
                    loading.showloading();
                } else {
                    loading.hideloading();
                }
            });


        $('#tbl-report-staff-member-remn-mansan-list tbody').on('click', 'tr', function () {
            $(this).toggleClass('selected');
        });


        $("#btn-search").on("click", function (e) {
            table.draw();
        });

        $("#btn-export-excel").on("click", function (e) {
            var fromDate = getFromDate();
            var toDate = getToDate();
            if (fromDate > toDate) {
                toastr.error('Từ ngày phải <= đến ngày');
                return;
            }

            $('#hdFromDate').val(fromDate);
            $('#hdToDate').val(toDate);
            $('#frm-export-excel').submit();

        });
  
        table_detail_order_list = $('#tbl-detail-order-by-offer-staff-transaction').DataTable({
            initComplete: function (settings, json) {
                $("#tbl-detail-order-by-offer-staff-transaction").wrap("<div style='overflow:auto; width:100%; position:relative;'></div>");
            },
            destroy: true,
            deferLoading: 0,
            select: {
                style: 'single'
            },
            dom: '<"top"iflp<"clear">>rt',
            paging: true,
            pagingType: "simple_numbers",
            responsive: false,
            processing: false,          // for show progress bar
            serverSide: true,           // for process server side
            filter: false,              // this is for disable filter (search box)
            orderMulti: false,          // for disable multiple column at once
            info: true,                 // Hiển thị dòng _START_ đến dòng _END_ của tổng số _TOTAL_ dòng,
            autoWidth: false,
            ordering: false,
            displayLength: 10,
            searching: false,
            lengthChange: false,
            scrollCollapse: false,

            ajax: {
                 url: '@Url.Action("GetDetailOfferStaffTransactionList", "CrownX")',
                 type: "POST",
                 datatype: "json",
                 data: function (d) {
                     d.StoreNo = $('#view-detail-store-no-temp').val();
                     d.OrderNo = $('#view-detail-order-no-temp').val();
                 }
            },

            columns: [

                    {
                        data: "Barcode",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.Barcode == null | row.Barcode == "") {
                                html += '<span style="font-weight:700 display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:center;">' + row.Barcode + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "ItemNo",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.ItemNo == null | row.ItemNo == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.ItemNo + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "Description",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.Description == null || row.Description == "") {
                                html += '<span style="display:block;text-align:left;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:left;">' + row.Description + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "UnitOfMeasure",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.UnitOfMeasure == null || row.UnitOfMeasure == "") {
                                html += '<span style="display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="display:block;text-align:center;">' + row.UnitOfMeasure + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "Quantity",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.Quantity == null || row.Quantity == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:center;">' + parseFloat(row.Quantity).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "UnitPrice",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.UnitPrice == null || row.UnitPrice == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:right;">' + parseFloat(row.UnitPrice).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "DiscountAmount",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.DiscountAmount == null || row.DiscountAmount == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:right;">' + parseFloat(row.DiscountAmount).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "VATAmount",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.VATAmount == null || row.VATAmount == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:right;">' + parseFloat(row.VATAmount).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "LineAmountIncVAT",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.LineAmountIncVAT == null || row.LineAmountIncVAT == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else {
                                html += '<span style="font-weight:700;display:block;text-align:right;">' + parseFloat(row.LineAmountIncVAT).toLocaleString('en') + '</span>';
                            }
                            return html;
                        }
                    },

                    {
                        data: "OfferNo",
                        render: function (data, type, row) {
                            var html = "";
                            if (row.OfferNo == null || row.OfferNo == "") {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + "" + '</span>';
                            }
                            else
                            {
                                html += '<span style="font-weight:400;display:block;text-align:center;">' + row.OfferNo + '</span>';
                            }
                            return html;
                        }
                    }
            ]

            }).on('processing.dt', function (e, settings, processing)
            {
                if (processing)
                {
                    loading.showloading();
                    var orderNo = $('#view-detail-order-no-temp').val();
                    $('#modal-title-detail-order').text('CHI TIẾT ĐƠN HÀNG - SO : ' + orderNo);
                }
                else
                {
                    loading.hideloading();
                }
                $('#tbl-detail-order-by-offer-staff-transaction tbody').on('click', 'tr', function () {
                    $(this).toggleClass('selected');
                });

            });



    });

        </script>
    }






